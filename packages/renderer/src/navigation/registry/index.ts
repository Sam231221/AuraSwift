/**
 * View Registry
 * Central export for view registry functions
 */

export {
  VIEW_REGISTRY,
  getView,
  getRootViews,
  getNestedViews,
  getViewHierarchy,
  canAccessView,
} from "./view-registry";

export type { ViewConfig } from "../types/navigation.types";


/**
 * Schema Entry Point
 *
 * This file re-exports all schema definitions from the modular structure.
 * It maintains backward compatibility with existing imports.
 *
 * For new code, consider importing directly from ./schema/index.js or specific modules.
 */

// Re-export everything from the modular schema
export * from "./schema/index.js";

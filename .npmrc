# NPM configuration for native module building
# Only include actual npm settings here

# Build configuration  
build-from-source=true
target_platform=win32
target_arch=x64
cache_max=5120000000
cache_min=10

# For native modules, use environment variables instead:
# export PYTHON=python
# export MSVS_VERSION=2022